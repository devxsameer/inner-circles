<!DOCTYPE html>
<html lang="en">
  <%- include("partials/head") %>
  <body
    class="h-screen flex flex-col bg-neutral-200 font-outfit overflow-y-auto"
  >
    <h1>Welcome to Inner-Circles</h1>
    <%- locals?.message %>
    <div class="border p-4">
      <% if(locals?.user){ %>
      <ul>
        <li>username: <%= user.username %></li>
      </ul>
      <a href="/auth/logout" class="underline">logout</a>
      <% }; %>
    </div>
  </body>
</html>
