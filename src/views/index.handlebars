<section class="max-w-6xl mx-auto px-4 py-16 text-center">
   <h1 class="text-4xl font-extrabold text-gray-900 mb-4">
      Welcome to <span class="text-rose-600">InnerCircles</span>
   </h1>
   <p class="text-gray-600 text-lg max-w-2xl mx-auto">
      Create circles. Share ideas. See the real people behind posts —
      only within your circles.
   </p>

   <div class="mt-8 flex justify-center gap-4">
      {{#if user}}
      <a href="/posts"
         class="btn-lg hover:bg-neutral-900">
         Browse Posts
      </a>
      <a href="/circles"
         class="btn-lg bg-neutral-100 border-neutral-950 text-neutral-950 hover:bg-neutral-200">
         Explore Circles
      </a>
      {{else}}
      <a href="/auth/signup"
         class="btn-lg hover:bg-neutral-900">
         Create Account
      </a>
      <a href="/auth/login"
         class="btn-lg  bg-neutral-100 border-neutral-950 text-neutral-950 hover:bg-neutral-200">
         Log In
      </a>
      {{/if}}
   </div>
</section>

<!-- ================= POPULAR CIRCLES ================= -->
<section class="bg-gray-50 border-neutral-200 border-t border-b py-14">
   <div class="wrapper">

      <h2 class="text-2xl font-bold mb-6">Popular Circles</h2>

      <div class="grid sm:grid-cols-2 md:grid-cols-3 gap-6">
         {{#each circles}}
         {{> circleCard}}
         {{/each}}
      </div>
   </div>
</section>

<!-- ================= PUBLIC POSTS FEED ================= -->
<section class="py-14">
   <div class="wrapper">
      <h2 class="text-2xl font-bold mb-6">Latest Public Posts</h2>

      <div class="grid md:grid-cols-2 gap-6">
         {{#each publicPosts}}
         {{#> postCard hideVisibility=true}}
         {{/postCard}}
         {{/each}}
      </div>
   </div>
</section>



<!-- ================= WHY JOIN SECTION ================= -->
<section class="py-16 bg-gray-50 border-neutral-200 border-t border-b">
   <div class="wrapper">
      <h2 class="text-2xl font-bold text-center mb-10">Why Join InnerCircles?</h2>

      <div class="grid md:grid-cols-3 gap-8">

         <div class="bg-white border border-neutral-200 hover:shadow-md  p-6 rounded-xl shadow-sm">
            <h3 class="font-semibold text-xl">🔒 Controlled Privacy</h3>
            <p class="text-gray-600 text-sm mt-2">
               Your identity is only visible inside the circles you join.
            </p>
         </div>

         <div class="bg-white border border-neutral-200 hover:shadow-md  p-6 rounded-xl shadow-sm">
            <h3 class="font-semibold text-xl">🏛 Create Your Own Circles</h3>
            <p class="text-gray-600 text-sm mt-2">
               Build communities instantly and manage members easily.
            </p>
         </div>

         <div class="bg-white border border-neutral-200 hover:shadow-md  p-6 rounded-xl shadow-sm">
            <h3 class="font-semibold text-xl">📝 Real Conversations</h3>
            <p class="text-gray-600 text-sm mt-2">
               Post publicly or members-only. Your circles, your rules.
            </p>
         </div>

      </div>
   </div>
</section>